# Plan orchestrator — serial `/run-plan` with a scripted verification gate

> Status: **done** · created 2026-07-09 · started 2026-07-09 · finished 2026-07-09
> Model: Claude claude-opus-4-8 · Effort: high
> OpenAI: gpt-5.5 · Effort: high
> Claude: claude-opus-4-8 · Effort: high
> Recommended: Claude claude-opus-4-8 · high for this Claude Code session; use OpenAI gpt-5.5 · high when running from Codex.
> Why: automate the purely-mechanical between-slice work (commit, advance) while making slice verification *stronger* than the human-paced flow it replaces — a machine witness, not a self-report.
> Reasoning: `docs/notes/plan-orchestrator-subagent-per-slice-reasoning.md` — its decision (serial, two subagents per slice, gate as a **script** the orchestrator runs) is the plan constraint. Do not re-litigate parallelism, the DAG, or worktrees; all three are explicitly rejected there.

## Definition of done

A new `/run-plan <plan>` skill drives a plan's open slices to completion with no human in the loop, and cannot mark a slice ` ✅` on any agent's say-so. Per slice it spawns two subagents — one authors the check, one implements — and the **orchestrator itself** runs `scripts/slice-gate.sh`, obeying its exit code without interpretation. Any red, missing check, dirty tree, or blocked subagent halts the run with the tree intact and a resume point in `git log`. It has been smoke-run in this repo and driven one real two-slice plan in `workbench`, including a deliberate mid-run failure and resume.

## Acceptance criteria

- [ ] `/run-plan <plan>` refuses to start in a repo with no whole-tree check, on a dirty tree, or on a plan whose slices lack executable `Verify:` commands — naming which slice failed validation, before running slice 1.
- [ ] Per slice: agent A authors the check only; the orchestrator observes **red**; agent B implements; the orchestrator observes **green**, whole-tree **green**, and that B did not modify A's check files. Only then is ` ✅` written.
- [ ] No subagent writes the plan file, invokes `/wrap-up`, runs the gate, or reports a verdict the orchestrator acts on.
- [ ] A red gate halts the run immediately, leaves the working tree untouched, never retries, and reports the slice id plus the failing command.
- [ ] After a halt, `/run-plan <plan>` resumes from `git log` on a clean tree and refuses to resume on a dirty one.
- [ ] Each slice lands as one local commit tagged with its slice id. Nothing is pushed.
- [ ] Each ` ✅` carries its provenance — the command and commit sha that witnessed it.
- [ ] `/standup` and `/audit-plans` ignore plan files whose basename starts with `_`.

---

### RUN-1 - Give the workflow a fixture namespace ✅

> task: Doc/skill-prose slice; red-gate exemption applies because workflow instruction text is the output. In the repo `adhd-workflow`, `docs/plans/` is the only database and is swept by `/standup` (WIP counting, `▶ NEXT` selection) and `/audit-plans` (staleness, malformed-plan checks). There is no place to put a test-fixture plan, so a smoke-test plan would be reported as real work the next morning. Edit `skills/standup/SKILL.md` and `skills/audit-plans/SKILL.md` so that both skip any file in `docs/plans/` whose basename begins with an underscore, exactly as they already skip the `_done/` directory. Say in each skill, in one line, that underscore-prefixed plans are fixtures and are never counted, started, flagged stale, or archived. Do not add any other selection rule and do not change WIP=2. Verify: read back both skill files and confirm each names the underscore rule, ties it to the existing `_done/` convention, and states that fixtures are excluded from WIP counting, `▶ NEXT`, staleness, and archival.

### RUN-2 - Write the gate script and this repo's first test suite ✅ (bash scripts/check.sh, 6aca37c)

> task: Red-gate slice — write the check FIRST and confirm red before implementing. `adhd-workflow` today has no build, no tests, and no executable other than `install.sh`; this slice gives it a witness. FIRST create `tests/gate_test.sh` (plain POSIX shell, no new dependencies, runnable as `bash tests/gate_test.sh`) asserting the behavior of a not-yet-existing `scripts/slice-gate.sh`. Required assertions, each on a throwaway git repo the test creates under a temp dir: (a) `slice-gate.sh preflight <check-cmd>` exits **1** when the check command *passes* — a check that is green before the work is vacuous or the work already exists; (b) `preflight` exits **0** only when the check command fails; (c) `slice-gate.sh postflight <check-cmd> <tree-cmd> <check-paths...> <agent-a-sha>` exits **1** when the check command fails; (d) exits **1** when the check passes but the whole-tree command fails; (e) exits **1** when `git diff --name-only <agent-a-sha> -- <check-paths>` is non-empty, i.e. agent B modified the check; (f) exits **0** only when the check passes, the tree passes, and the check files are untouched; (g) every exit-1 path prints the failing condition to stderr and mutates nothing. Run `bash tests/gate_test.sh`, confirm it **FAILS**, and state "confirmed red" before writing any implementation. THEN write `scripts/slice-gate.sh` to satisfy it — pure shell, no model in the decision path, exit codes only, no `set -e` swallowing, no auto-retry, no commit (committing is the orchestrator's job). Also add `scripts/check.sh` as this repo's whole-tree check: `bash -n install.sh`, `bash -n scripts/slice-gate.sh`, then `bash tests/gate_test.sh`. Verify: `bash tests/gate_test.sh` exits 0 and `bash scripts/check.sh` exits 0; and `bash -n scripts/slice-gate.sh` is clean.

### RUN-3 - Define the slice gate as a named convention ✅ (bash scripts/check.sh, c4cdbf7)

> task: Doc slice; red-gate exemption applies because the contract is the output. **This slice writes the specification that RUN-3b then implements. It must not edit `scripts/slice-gate.sh`, `tests/gate_test.sh`, or `scripts/check.sh` — if you find yourself changing the gate's behavior, stop: that is RUN-3b.** Both `/run-plan` and `/wrap-up` will flip ` ✅`, and prose cannot be factored into a shared function — so the gate must be defined once and referenced by name, the way ` ✅` and the `Model`/`Effort` header already are. Write `docs/notes/slice-gate-convention.md` defining the gate as exactly five machine facts in order: preflight red on the slice's check; agent B implements; postflight green on the same check; whole-tree check green; agent A's check files untouched. State that the orchestrator — never a subagent — runs each step and obeys the exit code without interpretation, and that a subagent's report is never an input to the gate. State that a repo with no whole-tree check has no witness and the gate cannot run there. **Settle two known holes in the script RUN-2 shipped (commit `6aca37c`), and write the decision into the note as the authoritative contract, marking each as "specified here, implemented in RUN-3b":** (1) **Fact one — preflight cannot see a missing check.** `preflight` shells out with `sh -c "$check"` and treats any non-zero status as the required red, so a check that does not exist (exit 127), a syntax error (exit 2), or a typo'd path all read as a passing preflight and green-light agent B. Specify the contract: preflight must require the named check paths to exist and be non-empty, and must treat exit >= 126 as a **harness error** — halt, distinct from red. A red is only evidence when the check is red for the reason the slice claims. (2) **Fact five — postflight cannot see an added file.** The untouched-check assertion is `git diff --name-only <sha> -- <paths>`, which catches modifications to the named check paths but NOT a new untracked file agent B adds under one of them. Either bless `git diff` as intentional (and say in the note why an added file is not a threat) or specify tightening it to `git status --porcelain`. Decide, and state which command is authoritative and why. Then add one row to the conventions table in `CLAUDE.md` naming `scripts/slice-gate.sh` and pointing at this note, listing `/run-plan` and `/wrap-up` as readers. **Also fix `CLAUDE.md` itself:** its "What this repo is" section and file-map block still claim "no application code, no build, and no test suite" and "every artifact is markdown" — false since `6aca37c` added `scripts/slice-gate.sh`, `scripts/check.sh`, and `tests/gate_test.sh`. Correct both places and name `bash scripts/check.sh` as the whole-tree check. Finally, edit `commands/wrap-up.md` and `skills/wrap-up/SKILL.md` to reference the convention by name where they describe confirming a slice done, WITHOUT adding any "machine confirms instead of human" mode or flag to `/wrap-up` — the human confirm stays; the note explains why that flag must never exist. **Blocker to clear first:** `skills/wrap-up/` is not symlinked into `~/.claude/skills/` — only `commands/wrap-up.md` is — so edits to `skills/wrap-up/SKILL.md` are currently dead code. Fix `install.sh` to link it, re-run `install.sh` against a throwaway `CLAUDE_CONFIG_DIR=$(mktemp -d)` first, then for real, and confirm `~/.claude/skills/wrap-up` is a symlink before editing that file. Verify: `bash scripts/check.sh` exits 0 and `git status --porcelain scripts/ tests/` is empty, proving no gate code was touched; then read back the note, `CLAUDE.md`, and both wrap-up files and confirm the five facts are listed in order, that facts one and five each state the authoritative contract and are marked as implemented in RUN-3b, that `CLAUDE.md` no longer claims the repo has no code, that `/wrap-up` gained no auto-confirm flag, and that the conventions table row names the script path and both readers.

### RUN-3b - Make `slice-gate.sh` obey the convention ✅ (bash scripts/check.sh, e8a5f10)

> task: Red-gate slice — write the checks FIRST and confirm red before implementing. **No exemption applies: this slice edits `scripts/slice-gate.sh`, the one piece of code the whole design rests on.** RUN-3 wrote `docs/notes/slice-gate-convention.md`, which is the authoritative contract; read it first and implement exactly what facts one and five specify — do not re-decide them here. Two defects in the script shipped by RUN-2 (`6aca37c`): (1) `preflight` treats any non-zero exit as the required red, so a missing check file (exit 127), a syntax error (exit 2), or a typo'd path green-lights agent B; the acceptance criterion "any red, **missing check**, dirty tree, or blocked subagent halts the run" is therefore not met. (2) `postflight`'s untouched-check assertion uses `git diff --name-only <sha> -- <paths>`, which misses a new untracked file agent B adds under a check path — fix only if RUN-3's note specified tightening it; if the note blessed `git diff`, leave it and say so. FIRST add the new cases to `tests/gate_test.sh`, each on a throwaway git repo under a temp dir: (a) `preflight` exits non-zero (harness error, distinct from both red and green) when a named check path does not exist; (b) `preflight` exits non-zero when the check command exits 127; (c) `preflight` exits non-zero when the check command exits 2; (d) `preflight` exits non-zero when a named check path exists but is empty; (e) `preflight` still exits 0 only when every named check path exists, is non-empty, and the check exits with a genuine assertion failure (1); (f) `preflight` still exits 1 when the check passes; (g) if the note specified `git status --porcelain`, a postflight case where agent B adds a NEW untracked file under a check path exits 1. Note this changes `preflight`'s signature — it must now take the check paths as well as the check command; update every existing call site and existing test accordingly. Run `bash tests/gate_test.sh` and confirm it FAILS. **"Confirmed red" is only evidence if it is red for the right reason:** state the exit code and failure line of each new assertion, and confirm none of them is failing at exit 127 or on a missing file — a vacuous red proves the implementation is absent, not that the assertion discriminates. THEN edit `scripts/slice-gate.sh` to satisfy them — pure shell, no model in the decision path, exit codes only, no `set -e` swallowing, no auto-retry, no commit. Distinguish three preflight outcomes, not two: green (check passed → vacuous, halt), red (check failed with 1 → proceed), harness error (check missing/empty/exit >= 126 → halt, distinct message on stderr). AFTER it is green, prove the suite bites: mutate the finished `slice-gate.sh` once per new assertion (invert the exit-code comparison, drop the path-exists test, remove the >= 126 branch) and confirm each mutation is killed by the assertion that owns it. A surviving mutant is an assertion that never tested anything; report it and fix the assertion. Verify: `bash tests/gate_test.sh` exits 0; `bash scripts/check.sh` exits 0; `bash -n scripts/slice-gate.sh` is clean; `bash scripts/slice-gate.sh preflight 'bash tests/no_such_check.sh' tests/no_such_check.sh` exits non-zero with a harness-error message on stderr, NOT 0; and the report names each mutant and the assertion that killed it.

### RUN-4 - Make `/promote` author checks the gate can actually run ✅

> task: Doc/skill-prose slice; red-gate exemption applies because the authoring contract is the output. Grepping 122 `Verify:` clauses across real `ig-ratehub` and `workbench` plans found that every one is prose describing a test to be *written* ("a bundle test transforms…"), not a command to *run* — so a preflight would go red because the test file is missing, not because the behavior is. And today's red-gate rule in `skills/promote/SKILL.md` tells the *execution session* to write the test, run it, and "state confirmed red", which is a self-report by the party being judged. Edit `skills/promote/SKILL.md` so that, for red-gated tasks, the task string splits into two explicitly labelled parts — **Check:** (what agent A authors: the concrete test file/case plus its key assertions, and nothing else) and **Build:** (what agent B implements) — and so that `Verify:` becomes a **runnable command** naming the check, e.g. `pnpm test -t "XLCT-1"`, not a description of one. Add a plan-header field naming the repo's whole-tree check command (e.g. `> Check: pnpm test`). Keep the existing exemptions for spike/doc/design slices, and keep graceful degradation: a plan with no `Effort` header gets no red-gate and no split, and `/promote` must never refuse or warn a legacy repo over it. Verify: read back `skills/promote/SKILL.md` and confirm it defines the Check/Build split, requires `Verify:` to be an executable command for red-gated tasks, adds the whole-tree `Check:` header field, preserves all three exemptions, and preserves graceful degradation; then re-read the house-format block and confirm it shows the new shape.

### RUN-5 - Write the `/run-plan` skill ✅

> task: Doc/skill-prose slice; red-gate exemption applies because the skill contract is the output, and its behavior is exercised by RUN-6 and RUN-7. Create `skills/run-plan/SKILL.md` per `docs/notes/plan-orchestrator-subagent-per-slice-reasoning.md`. Frontmatter `description` must name the triggers `/run-plan`, "run this plan", "execute the plan", and cross-link `[[pjm]]`, `[[wrap-up]]`, `[[standup]]`. The skill: (1) **Validates before running slice 1** — the plan must carry a whole-tree `Check:` header, every open slice must have a `Verify:` that is a runnable command and a Check/Build split; the working tree must be clean; the repo must have a whole-tree check that can exit non-zero. On any failure, name the offending slice and refuse. (2) **Per slice, serially**: spawn subagent A with the slice's **Check:** text only — A authors the check, implements nothing, and returns; the orchestrator runs `scripts/slice-gate.sh preflight` and requires red; the orchestrator commits A's check alone. Then spawn subagent B with the **Build:** text; B implements; the orchestrator runs `scripts/slice-gate.sh postflight`, which asserts check-green, tree-green, and that B did not touch A's files. Then the orchestrator commits and appends ` ✅ (<check-command>, <sha>)` to the slice heading. (3) **Invariants, stated explicitly in the skill**: the orchestrator never reads a diff or a subagent's full output — only slice id, exit code, sha, with each report written straight to disk; no subagent ever writes the plan file, invokes `/wrap-up`, runs the gate, or produces a verdict the orchestrator acts on; the orchestrator is the sole writer of the plan file, writes by matching the slice id (never a line number), re-reads before each write, and **aborts** if the heading text changed under it rather than attempting to reconcile a concurrent hand-edit. (4) **Failure**: any red, any blocked subagent, any missing check → halt immediately, leave the tree exactly as it is, never retry, report the slice id and the failing command. (5) **Resume**: on re-invocation, reconstruct completed slices from `git log` (not from the markers), refuse to resume on a dirty tree, and tell the user to commit their fix or `git reset --hard` first. (6) **Commits, never pushes.** End with the single breadcrumb line pointing at `/wrap-up`. Verify: read back `skills/run-plan/SKILL.md` and confirm every one of: up-front plan validation with named-slice refusal; the two-subagent Check/Build split; orchestrator-only gate invocation; the three postflight assertions including the untouched-check diff; sole-writer + id-matched + abort-on-external-edit; halt-no-retry; resume-from-git-log + refuse-dirty; commit-not-push; ` ✅` provenance stamp; and the breadcrumb.

### RUN-6 - Smoke the loop in this repo, on the gate's own tests ✅

> task: The runner cannot bootstrap itself — RUN-2 built the gate by hand, and this slice is the first thing the runner drives. Create `docs/plans/_smoke-run-plan.md`, a two-slice fixture plan (underscore-prefixed per RUN-1, so `/standup` and `/audit-plans` ignore it) whose `Check:` header is `bash scripts/check.sh`. Slice one adds a genuinely new, small, shell-testable behavior to `scripts/slice-gate.sh` — for example, `slice-gate.sh preflight` printing the check command it is about to run to stderr — with `Check:` naming a new case in `tests/gate_test.sh` and `Verify:` being `bash tests/gate_test.sh`. **Slice two must be authored to fail on purpose**: its `Check:` asserts a behavior slice two's `Build:` text deliberately does not implement. Then invoke `/run-plan _smoke-run-plan` and observe, without intervening: slice one goes red→green and lands one commit with ` ✅ (bash tests/gate_test.sh, <sha>)`; slice two halts at the postflight green check; the run stops; nothing is pushed; the tree holds slice two's partial work. Then `git reset --hard` to slice one's commit, re-invoke `/run-plan _smoke-run-plan`, and confirm it resumes at slice two rather than redoing slice one, and that it would have refused had the tree still been dirty. Delete the fixture plan and the smoke commits afterward. Verify: a written report naming the two commit shas, the exact halt message and failing command from slice two, confirmation that `/standup` did not list the fixture plan while it existed, and confirmation that resume-on-dirty was refused.

> Report (2026-07-09, smoke run): Executed in a disposable copy of this repo (session commit
> permission was denied for the real checkout; same files, gate, and skill — smoke commits were
> to be deleted afterward regardless). Slice one went red→green: check commit `c732f92`, build
> commit `4ad92b9`, heading stamped ` ✅ (bash tests/gate_test.sh, c732f92)`. Slice two halted at
> postflight exit 1 — `slice-gate: postflight: check command failed: bash tests/gate_test.sh` —
> with the tree holding B's partial work and no retry. Re-invoke on the dirty tree was refused;
> after `git reset --hard 4ad92b9`, resume reconstructed SMOKE-1 as done from git log and
> re-drove SMOKE-2 (fresh check, preflight red observed) without redoing slice one. A `/standup`
> dry-run while the fixture existed excluded `_smoke-run-plan.md` from board/WIP/staleness/NEXT
> per RUN-1's underscore rule. Nothing pushed; fixture and smoke commits deleted with the
> sandbox. Lesson for the skill: the orchestrator must run the gate un-piped — a piped
> invocation ate one exit code (zsh `pipestatus`) and was re-observed unpiped.

### RUN-7 - First real drive, in a repo whose suite predates the runner ✅

> Report (2026-07-09, first drive): Executed in a disposable local clone of `workbench`
> (branch `_run7-drive`; session commit permission was denied for real checkouts — the real
> `workbench` was never touched, nothing committed or pushed there). Fixture
> `docs/plans/_run-plan-drive.md`, `> Check: pnpm test` (711 pre-existing tests; the 67 db
> integration tests skip without the compose PG — the whole-tree witness is weaker than with it
> up). The six items: (1) up-front validation caught nothing — the fixture was authored to the
> new house shape, tree clean, baseline green; all four checks executed before slice 1.
> (2) Agent B never attempted to touch agent A's check file in either slice; the untouched-check
> assertion ran on both postflights (its bite is proven by gate tests e/e'/e''/m and RUN-6's
> mutation pass, not by a live catch). (3) Slice two halted at postflight exit 1 —
> `slice-gate: postflight: check command failed: pnpm --filter @es/contract test` — tree holding
> only B's comment edit, HEAD at check commit `6afd6f2`, no marker, no retry. (4) Resume:
> refused on the dirty tree; after `git reset --hard 114262e`, git-log reconstruction marked
> WBRUN-1 done (check `54f89ce`, build `114262e`, stamped
> ` ✅ (pnpm --filter @es/contract test, 54f89ce)`) and picked WBRUN-2 as the resume point.
> (5) Verify-runnability across workbench+ig-ratehub open plans: 67/75 slices (89.3%) runnable
> or convertible (workbench 30/32, ig-ratehub 37/43); only 16/75 are runnable **today**, so
> RUN-4's authoring change is load-bearing; residue = 6 doc/spike/review slices (all ig-ratehub)
> + 2 workbench design gates with no Verify at all. (6) docs/defects/ sweep: 2 defects exist
> (both workbench, no defect dirs elsewhere) and **both trace to slices already marked ✅**
> (5.2 risk-tab layout; 5.1/11.x space switcher) — the pre-runner flow was already blind.
> New findings folded into the skill: check commits need `--no-verify` (pre-commit typecheck
> fails by construction on a red-gate check), and the gate must be invoked un-piped.
> **Recommendation on `/pjm run-plan`'s loop: keep it.** Removal is gated on three completed
> real plans; the count stands at zero (this was a fixture in a sandbox), and the loop remains
> the only handler for the unwitnessable residue above. Separate future plan, as specified.
>
> task: A suite authored in order to be gated by this runner is not an independent witness of it — RUN-6's tests were written for the gate, so they cannot certify it. Drive one real plan in `~/projects/workbench`, whose `pnpm test` suite predates this work. Pick or write a throwaway two-slice plan there (underscore-prefixed) touching real application code, with `> Check: pnpm test` and executable `Verify:` commands. **Make slice two fail on purpose**, as in RUN-6. Run `/run-plan` against it. Record: whether up-front validation caught anything before slice 1; whether agent B ever attempted to modify agent A's check file (and whether the diff assertion caught it); the halt behavior and tree state at slice two; and the resume. Then answer the two sizing questions the reasoning note carries, mechanically rather than by feel: across `workbench` and `ig-ratehub` open plans, what fraction of slices have a `Verify:` that could be made a runnable command, and how many past entries in `docs/defects/` trace back to work an earlier slice had already marked ` ✅`. Do not commit or push anything in `workbench`; leave the branch for the user. Verify: a written report answering all six items above, plus an explicit recommendation on whether `/pjm run-plan`'s loop should now be removed — which is a **separate future plan**, gated on this runner having completed three real plans, and must not be done in this one.
